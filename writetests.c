#include "types.h"
#include "stat.h"
#include "user.h"

int
main(int argc, char *argv[])
{
  //int i;
  printf(1, "%d", writecount());
  printf(1, "\n");
//  for(i = 1; i < argc; i++)
	//printf(1,
    //printf(1, "%s%s", argv[i], i+1 < argc ? " " : "\n");
	//printf(1, "%u", writecount(), "\n");
  exit();
}
